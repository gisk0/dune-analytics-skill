{
  "name": "dune-analytics-skill",
  "version": "0.1.0",
  "description": "OpenClaw skill for Dune Analytics API — run SQL queries against blockchain data",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "lint": "trunk check --fix"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "eslint": "^10.0.0",
    "globals": "^16.0.0",
    "prettier": "^3.5.0",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.30.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
